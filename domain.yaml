domain_map:
  .maestro-core:
    description: Framework de context routing — diretivas, templates e scripts do sistema maestro

global_rules:
  never_load:
    - "**/raw/**"

context_routing:
  .maestro-core:
    always_load:
      - ./.maestro-core/context/playbook.md
